/// <reference types="vite/client" />

// Declares the ambient module types Vite provides for side-effect asset
// imports such as `import './globals.css'`. These used to come in through
// vinext/types; the framework is gone, so reference Vite's own types.
