import assert from 'node:assert/strict';import{readFile,stat}from'node:fs/promises';const h=await readFile(new URL('../index.html',import.meta.url),'utf8');const t=await readFile(new URL('../achiote-lime-page-code.txt',import.meta.url),'utf8');assert.equal(h,t);assert.match(h,/achiote/);assert.match(h,/data-burner/);assert.match(h,/165/);assert.ok((await stat(new URL('../images/achiote-lime-grilled-boneless-chicken.webp',import.meta.url))).size<250000);console.log('Achiote-lime package smoke checks passed.');
