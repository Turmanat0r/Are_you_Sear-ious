import assert from 'node:assert/strict';import{readFile,stat}from'node:fs/promises';const h=await readFile(new URL('../index.html',import.meta.url),'utf8');const t=await readFile(new URL('../burger-page-code.txt',import.meta.url),'utf8');assert.equal(h,t);assert.match(h,/Worcestershire/);assert.match(h,/bison/);assert.match(h,/data-burner/);assert.match(h,/160/);assert.ok((await stat(new URL('../images/steakhouse-beef-bison-burgers.webp',import.meta.url))).size<250000);console.log('Burger package smoke checks passed.');
